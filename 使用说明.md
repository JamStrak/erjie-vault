# 二姐小金库

这是给二姐杂货铺两位合伙人用的手机账本。第一版由一部手机负责记账，另一位通过导出的摘要或 CSV 明细核对。网页没有云端账目数据库；同一网址在另一台手机打开，不会自动出现这台手机的记录。

## 第一次使用

1. 用 iPhone 的 Safari 打开发布后的 HTTPS 地址，通过“分享 → 添加到主屏幕”建立“二姐小金库”图标。之后固定从这个图标打开和记账。安卓也能打开网址；如果要由安卓接手记账，请先在原手机“设置 → 导出完整备份”，再在安卓导入 JSON，确认余额一致后只在安卓继续录入，避免两套账本分叉。
2. 在“设置”填写下次计划进货额与想留在金库里的备用金。它们只是计划，不会写成已经发生的流水。
3. 合伙人真实存入一笔钱，就选“入金 → 只记一人”，选对名字。你自己真正存入后，再记自己的入金。只有两人的钱都已经进入金库，才能选“两人都已入金，金额相同”。
4. 每次进货选“进货”，注明从金库付还是某人个人垫付；每次卖出商品选“销售记录”，注明收款已进金库还是由某位合伙人代收。个人代收的款实际转进金库后，在“其他业务 → 代收销售款转入金库”记录转入。进货备注可写品名、数量，方便以后核对。
5. 每次出摊后，点右上角图标导出**完整 JSON 备份**。iPhone 若弹出系统分享菜单，选“存储到文件”；其他浏览器可能直接下载。务必打开手机“文件”确认 JSON 确实存在，再视需要把 CSV 明细发给合伙人。CSV 开头有导出时点的余额、出资和待处理金额，下面是完整流水；它用来阅读，恢复完整账本必须使用 JSON。设置页的备份日期只表示发起过导出，不证明文件已保存。

## 钱怎么算

- 金库可用余额＝两人的实际入金＋已进入金库的销售和其他收入＋后来转入金库的代收销售款－金库已付进货与其他支出－已报销垫付－已返还出资。个人代收但还没转入的销售款另列，不算可用余额，转入时也不会重复计入累计销售。
- 累计净出资只算已实际入金、明确转为出资的个人垫付，再减已返还的出资。销售收入不算任何一人的出资。
- 某人个人付了进货/费用，账本记一笔“待报销”，金库现金与该人的出资额暂时都不变。真正从金库还钱时再记“报销个人垫付”。双方若明确决定这笔垫付不再报销，才记“垫付转为出资”。这两种处理不能对同一金额重复做。
- 计划进货、备用金和待报销款共同构成当前资金目标。先让出资少的一方补平差额；若金库还不够，剩余缺口两人各补一半。展示的是**建议金额**，不会自动生成未到账的流水。
- 金库余额够不够进货，是资金可用性判断。进的货仍是商品资产，余额减少不等于已经亏损；本版不计算库存或净利润。
- 金库支出、报销、返还出资不能让**当前账面余额**变成负数。补记较早日期的流水时，本版不会逐日重建当时的现金余额；请按真实发生日期录入并人工核对历史顺序。如果保存被拒绝，请核对是否漏记入金、销售，或这笔钱其实是某人垫付。

## 纠错与备份

在“明细”里可作废记错的记录，再重新记一笔。作废项仍留在历史与导出的 CSV 中；如果作废会让金库余额、出资或待报销变成负数，系统会阻止该操作，先更正后续相关记录。

完整备份仅保存在你选定的手机文件位置。浏览器或系统清理网站数据、更换手机、切换浏览器/主屏图标，都可能让原本保存在设备上的账本不可见；请定期导出 JSON 并保存在你自己能找回的地方。导入 JSON 会替换当前设备上的全部记录；确认前会显示新旧账本的记录数和最新录入时间。应用会在本机暂存导入前的账本，可从设置撤销上次导入，但下一次导入会覆盖这份暂存，清理网站数据也会一并清除，所以仍要先导出当前账本。

程序文件可以公开部署，**账目记录不会上传到部署网站**。他人知道网址只能打开自己的空白账本；如果你把完整 JSON 或 CSV 发给他人，文件中的经营账目也会随之分享。

## 开发与检查

页面是静态 HTML/CSS/JavaScript，不需要账号或额外 npm 依赖。`ledger.js` 包含以“分”为单位的计算与校验；`app.js` 管理浏览器本地账本和导入导出。使用 `npm test` 运行核心账目测试。维护预览可在本目录用 `python -m http.server 22861 --bind 127.0.0.1`，正式手机使用应使用固定 HTTPS 地址。
